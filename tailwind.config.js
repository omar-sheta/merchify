/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './pages/**/*.{js,jsx}',
    './components/**/*.{js,jsx}',
  ],
  // Tailwind v4 uses @theme in CSS instead of config
  plugins: [],
}
