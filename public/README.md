Place static assets here. Add a short sample video named `sample-comedy.mp4` to allow local testing of the video player.

Suggested steps:
- drop `sample-comedy.mp4` into this folder
- refresh the app at http://localhost:3000
